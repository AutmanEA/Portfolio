services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: portfolio-front-app
    ports:
      - "4200:4200"
    volumes:
      - ./frontend:/app
      - /app/node_modules
